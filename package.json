{
  "name": "addictd-web",
  "version": "0.0.0",
  "private": true,
  "description": "addictd.ai — V3 hero (static site, Tron direction)",
  "scripts": {
    "dev": "live-server --port=8000"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
